export function ProjectTag({ children }) {
  return (
    <span className="rounded-md border border-border bg-background px-2.5 py-1 text-xs font-medium text-foreground-muted">
      {children}
    </span>
  );
}
